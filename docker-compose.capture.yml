# Overlay για packet capture — απαιτεί Linux host
# Χρήση: docker-compose -f docker-compose.yml -f docker-compose.capture.yml up -d

services:
  app:
    network_mode: host
    cap_add:
      - NET_ADMIN
      - NET_RAW
    environment:
      AUTO_START_CAPTURE: "true"
